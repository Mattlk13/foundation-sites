{
  "$schema": "../../../schema/manifest.schema.json",
  "name": "stack",
  "kind": "layout",
  "group": "Boxes and Stacks",
  "description": "Stacks its children vertically with one consistent gap between them.",
  "class": "stack",
  "attributes": [
    {
      "name": "data-gap",
      "type": "enum",
      "vocabulary": "gap",
      "default": "md",
      "description": "Space between children. A pair such as sm-lg grows fluidly from the first stop to the second."
    },
    {
      "name": "data-align",
      "type": "enum",
      "vocabulary": "align",
      "default": "stretch",
      "description": "Horizontal alignment of the children."
    },
    {
      "name": "data-fill",
      "type": "boolean",
      "description": "Make the stack at least as tall as the viewport, so a child carrying data-split reaches the bottom."
    },
    { "name": "data-rule", "type": "boolean", "description": "A line between children, of the border width in the border color, in the middle of each gap. The gap is unchanged." }
  ],
  "classes": [],
  "children": [
    {
      "selector": "> *",
      "min": 1,
      "max": null,
      "description": "Anything. Each child's margins are reset; the stack owns the space between them."
    },
    {
      "selector": "> [data-split]",
      "min": 0,
      "max": 1,
      "description": "At most one child may carry data-split; it and everything after it are pushed to the end of the stack when the stack is taller than its content."
    }
  ],
  "markers": [
    {
      "name": "data-split",
      "type": "boolean",
      "on": "> *",
      "description": "Pushes the child and everything after it to the end of the stack when the stack is taller than its content."
    },
    {
      "name": "data-space",
      "type": "enum",
      "vocabulary": "gap",
      "on": "> *",
      "description": "The gap before this one child, larger or smaller than the stack's own: more room above a heading, less between a label and its field. Does nothing on the first child; data-split wins on a child carrying both."
    },
    {
      "name": "data-sticky",
      "type": "boolean",
      "on": "> *",
      "description": "Pins this child at --yeti-sticky-offset from the top of the scrollport while the rest of the stack scrolls past it. It keeps the full width of the column, a stack stretching its children on the inline axis."
    }
  ],
  "tokens": [
    {
      "name": "--yeti-space-md",
      "public": true,
      "description": "The default gap."
    },
    {
      "name": "--yeti-cover-height",
      "public": true,
      "description": "The minimum block size when data-fill is set."
    },
    {
      "name": "--yeti-border-width",
      "public": true,
      "description": "Width of the data-rule line."
    },
    {
      "name": "--yeti-color-border",
      "public": true,
      "description": "Color of the data-rule line."
    },
    {
      "name": "--_yeti-gap",
      "public": false
    },
    {
      "name": "--_yeti-align",
      "public": false
    },
    {
      "name": "--_yeti-stack-gap",
      "public": false
    },
    {
      "name": "--_yeti-rule-at",
      "public": false
    }
  ],
  "a11y": {
    "requiredAttributes": [],
    "keyboard": [],
    "notes": "Purely visual. Reading order is source order."
  },
  "js": null,
  "support": {
    "unguarded": [
      "flexbox gap"
    ],
    "guarded": []
  },
  "since": "7.0.0",
  "example": "example.html"
}
