{
  "$schema": "../../../schema/manifest.schema.json",
  "name": "dialog",
  "kind": "component",
  "group": "Forms and Actions",
  "description": "The native dialog element as a modal, opened by a button whose commandfor names it, with the page behind it inert and focus held inside.",
  "class": "dialog",
  "attributes": [
    { "name": "data-max", "type": "enum", "vocabulary": "width", "default": "md", "description": "The widest the dialog may grow, from the width scale; it is always narrower than the viewport by a gutter." }
  ],
  "classes": [],
  "children": [
    { "selector": "> *", "min": 1, "max": null, "description": "The contents: a heading that names the dialog, then whatever it is for." },
    { "selector": "> footer", "min": 0, "max": 1, "description": "The buttons, at the end edge. A button inside a form with method=\"dialog\" closes it with no script." }
  ],
  "tokens": [
    { "name": "--yeti-dialog-surface", "public": true, "description": "Background of the dialog." },
    { "name": "--yeti-dialog-radius", "public": true, "description": "Corner of the dialog." },
    { "name": "--yeti-dialog-padding", "public": true, "description": "Padding inside." },
    { "name": "--yeti-dialog-backdrop", "public": true, "description": "The wash over the page behind it." },
    { "name": "--yeti-shadow-md", "public": true, "description": "The dialog's shadow." },
    { "name": "--yeti-space-md", "public": true, "description": "The least margin kept between the dialog and the viewport's edges." },
    { "name": "--yeti-color-text", "public": true, "description": "The dialog's text." },
    { "name": "--yeti-width-md", "public": true, "description": "The default maximum, when data-max is absent." },
    { "name": "--yeti-space-sm", "public": true, "description": "Gap between the footer's actions." },
    { "name": "--yeti-duration-fast", "public": true, "description": "How long the dialog and its backdrop take to appear." },
    { "name": "--yeti-ease", "public": true, "description": "The curve of that transition." },
    { "name": "--_yeti-max", "public": false }
  ],
  "a11y": {
    "requiredAttributes": [],
    "keyboard": [
      { "key": "Escape", "action": "Closes the dialog." },
      { "key": "Tab", "action": "Cycles inside the dialog, since the rest of the page is inert." }
    ],
    "notes": "Give the dialog a name with aria-labelledby pointing at its heading. A button with commandfor naming the dialog and command=\"show-modal\" opens it modally: the browser makes everything behind it inert, holds focus inside, and closes on Escape; none of that needs ARIA or a script. A button inside a form with method=\"dialog\" closes it without any script. dialog.js adds a backdrop click that closes the dialog and returns focus to the opening button on close; without it the dialog still opens and Escape still closes it."
  },
  "js": [{ "module": "dialog.js", "optional": true, "events": [
    { "name": "yeti:open", "description": "Dispatched on the dialog once the browser has opened it from a commandfor button." },
    { "name": "yeti:close", "description": "Dispatched on the dialog when it closes, however it was closed." }
  ] }],
  "support": { "unguarded": ["invoker commands (commandfor and command)", "dialog", "::backdrop", "@starting-style", "transition-behavior: allow-discrete"], "guarded": [] },
  "since": "7.0.0",
  "example": "example.html",
  "demo": { "height": "xl" }
}
